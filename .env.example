# Copy this file to ".env" and fill your secrets.
# Lines formatted as KEY=VALUE (quotes optional). No spaces around '=' recommended.

# OpenAI API Key (used when Engine = openai). If left blank in UI, this value is used.
OPENAI_API_KEY=sk-your-openai-key-here
